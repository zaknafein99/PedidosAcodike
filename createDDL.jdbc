CREATE TABLE pedidos.pedidos (id INTEGER AUTO_INCREMENT NOT NULL, total INTEGER, ENTREGADO CHAR(1), fecha_pedido DATETIME, observaciones TEXT(64000), flete INTEGER, cantidad INTEGER, idcliente INTEGER, id_movil INTEGER, movilasignado INTEGER, id_articulo INTEGER, id_movil2 INTEGER, PRIMARY KEY (id))
CREATE TABLE pedidos.moviles (nromovil INTEGER NOT NULL, observaciones VARCHAR(255), PRIMARY KEY (nromovil))
CREATE TABLE pedidos.cliente (ID INTEGER AUTO_INCREMENT NOT NULL, TELEFONO VARCHAR(255), TIPOCLIENTE VARCHAR(255), DIRECCION VARCHAR(255), ESTADO CHAR(1), apellidonombre VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE pedidos.articulos (codigo INTEGER NOT NULL, precio INTEGER, kgs INTEGER, nombre TEXT(64000), PRIMARY KEY (codigo))
ALTER TABLE pedidos.pedidos ADD CONSTRAINT FK_pedidos_id_movil FOREIGN KEY (id_movil) REFERENCES pedidos.moviles (nromovil)
ALTER TABLE pedidos.pedidos ADD CONSTRAINT FK_pedidos_movilasignado FOREIGN KEY (movilasignado) REFERENCES pedidos.moviles (nromovil)
ALTER TABLE pedidos.pedidos ADD CONSTRAINT FK_pedidos_idcliente FOREIGN KEY (idcliente) REFERENCES pedidos.cliente (ID)
ALTER TABLE pedidos.pedidos ADD CONSTRAINT FK_pedidos_id_articulo FOREIGN KEY (id_articulo) REFERENCES pedidos.articulos (codigo)
ALTER TABLE pedidos.pedidos ADD CONSTRAINT FK_pedidos_id_movil2 FOREIGN KEY (id_movil2) REFERENCES pedidos.moviles (nromovil)
